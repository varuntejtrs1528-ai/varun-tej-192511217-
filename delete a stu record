#include<stdio.h>
struct s { int x; };

void main(){
    struct s a[5]={{1},{2},{3},{4},{5}};
    int n=5,i,pos=2;

    for(i=pos;i<n-1;i++)
        a[i]=a[i+1];

    n--;

    for(i=0;i<n;i++)
        printf("%d ",a[i].x);
}
