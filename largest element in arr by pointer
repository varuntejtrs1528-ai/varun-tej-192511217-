#include<stdio.h>

void main()
{
    int arr[5] = {10, 25, 5, 40, 15};
    int *p = arr;
    int i, max = *p;

    for(i = 1; i < 5; i++)
    {
        if(*(p+i) > max)
            max = *(p+i);
    }

    printf("Max = %d", max);
}
