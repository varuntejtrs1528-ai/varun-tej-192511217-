#include<stdio.h>

int sum(int arr[], int n)
{
    int i, s = 0;
    for(i = 0; i < n; i++)
        s += arr[i];
    return s;
}

void main()
{
    int arr[5] = {1,2,3,4,5};
    printf("Sum = %d", sum(arr,5));
}
