#include <stdio.h>
struct student { int marks; };

int main() {
    struct student s[3]={{50},{80},{70}};
    int max=s[0].marks;
    for(int i=1;i<3;i++)
        if(s[i].marks > max)
            max=s[i].marks;
    printf("%d", max);
}
