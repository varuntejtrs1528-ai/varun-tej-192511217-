#include<stdio.h>
struct s { int x; };

int freq(struct s a[],int n,int key){
    int i,c=0;
    for(i=0;i<n;i++)
        if(a[i].x==key) c++;
    return c;
}

void main(){
    struct s a[5]={{1},{2},{1},{3},{1}};
    printf("%d",freq(a,5,1));
}
