#include<stdio.h>
void main()
{
int num,x,digit,pallindrome=0;
while (num!=0)
{ x=num;
digit = num%10;
pallindrome*=digit;
num/=10;
}
if(x==pallindrome){
printf("num is pallindrome")}
else{
printf("num is not a pallinndrome)}
